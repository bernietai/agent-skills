---
name: browserbase-1password
description: Browserbase extension with integration to 1Password for secure credential management and autofill capabilities.
metadata: { "openclaw": { "requires": { "bins": ["openclaw", "curl", "node", "npm"] } } }
---

# Environment variables 

To use the Browserbase extension with 1Password integration, ensure the following environment variables are set:

- BROWSERBASE_PROJECT_ID
- BROWSERBASE_API_KEY
- OP_SERVICE_ACCOUNT_TOKEN

